## Running the project

The repository does not contain pre-trained models or checkpoints.  
If you want to reproduce the results, you will need to train the model from scratch.

Training usually takes around 1–2 hours depending on the setup (especially if using Colab GPU).

I chose not to include saved models so that the full training process can be reproduced and observed.
